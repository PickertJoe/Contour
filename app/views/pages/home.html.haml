.container-fluid
  .row.no-gutters
    .col
      =image_tag("Green.jpg", class: 'img-fluid')
    .col
      =image_tag("Gray.jpg", class: 'img-fluid')
    .col
      =image_tag("Stream.jpg", class: 'img-fluid')
  .row.home-banner.no-gutters
    .col
      Your favorite adventures, seen in a whole new way.
  .row.no-gutters
    .col
      =image_tag("Topo.png", class: 'img-fluid')
    .col
      =image_tag("Plot.png", class: 'img-fluid')
  .row-no-gutters
    .col.text-center
      - if user_signed_in?
        %button.btn.btn-light.btn-lg= button_to "Let's create!", new_user_gpx_path(current_user), :method => "get"
      - else
        %button.btn.btn-light.btn-lg= button_to "Let's create!", new_user_session_path

